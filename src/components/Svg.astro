---
export interface Props {
  class?: string;
  file: string;
}

const { class: className, file } = Astro.props as Props;
const { default: innerHTML } = await import(`/src/assets/${file}.svg?raw`);
---

<Fragment set:html={innerHTML} class={className} />
